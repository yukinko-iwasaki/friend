
  <div class=goalsetting>
  <h2>ゴール設定</h2>




<button id="opener">時間で設定</button>
<button id="opener2">チェックで設定</button>

<p>
習慣づけが目的の方はチェック方式
<br>具体的な目標値が決まっている方は数値方式が便利です
  </p>
  </div>
  <div id="dialog" title="ゴール設定/数字で設定">
    <%= form_for @goal, :url => {:action => :create}, remote: true do |f| %>
  <%=f.text_field :goal_name, :placeholder=>"ジョギング" %>を 
   <%= f.text_field :deadline, :placeholder=>"4/30", class: "datepicker" %>
  までに
  <%=f.text_field :quantity, :placeholder=>"５"%> 
  <%=f.text_field :qunit, :placeholder=>"キロ" %>
  <p>達成する</p>

  <div class="col10 push1">
              <%= button_tag  type: "submit", class: "btn btn--block",id:"heart3" do %>
              <p id="setting"> 設定</p><i class="icon-arrow-right"></i>
              <% end %>
            </div>
  <% end %>

  </div>


<div id="dialog2" title="ゴール設定/チェックリスト">
    <%= form_for @goal, :url => {:action => :createcheck}, remote: true do |f| %>
  <%=f.text_field :goal_name, :placeholder=>"ジョギング５キロ" %>を<br>
  
  <%= f.select :frequency_unit, [["毎日", "日"], ["毎週","週"],["毎月", "月"]]%>
  <%=f.text_field :quantity, :placeholder=>"５"%>回<br>
  <%= f.text_field :deadline, :placeholder=>"4/30", id:"datepicker2" %>まで
  継続する

  <div class="col10 push1">
              <%= button_tag  type: "submit", class: "btn btn--block" ,id: "heart3" do %>
              <p id="setting"> 設定</p><i class="icon-arrow-right"></i>
              <% end %>
            </div>
  <% end %>

  </div>



<script>
$("#dialog").css('background-color','#ffffb7');
 $("#dialog" ).dialog({ autoOpen: false,
  minWidth: 800, minHeight: 700});
  $("#opener").click(function(){
  $("#dialog").dialog("open");
  });
  $("#dialog").submit(function(){
  $(this).dialog("close");
  
  });

  $(function(){
     var dateFormat = 'yy-mm-dd'
    $(".datepicker").datepicker({
      dateFormat: dateFormat
    });
  })
$("#dialog2").css('background-color','#ffffb7');
  $("#dialog2" ).dialog({ 
  autoOpen: false,
  minWidth: 800, minHeight: 700,});
  $("#opener2").click(function(){
  $("#dialog2").dialog("open");
  });
  $("#dialog2").submit(function(){
  $(this).dialog("close");
  });

  $(function(){
     var dateFormat = 'yy-mm-dd'
    $("#datepicker2").datepicker({
      dateFormat: dateFormat
    });
  })




</script>
