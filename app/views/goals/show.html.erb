マイゴール詳細ページ

<%= link_to '進捗データ入力',  new_user_goal_track_path(goal_id: params[:id]) %>

  <p>プログレスは
  <% @progs.each do |prog| %>
  <% @total += prog.quantity %>
  <% end %>

 

  <%= @total/@aim.quantity*100  %>
  %です</p>

<div class=comments>
<%= form_for @comment, :url=>{:action=> :commentcreate}, remote: true do |f| %>
<%= f.text_field :comment %>
<%= f.submit 'コメント' %>
<% end %>



<ul class=comment_post>
 
  <%= render @comments %>

</ul>


  </div>
