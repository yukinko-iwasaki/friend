<div class="mygoals">
  <p class="usertitle"><%= @name %> さんの目標一覧!</p>

  </div>
  <div class="invitation_container">
  <% if @user.id==current_user.id %>
  <% if @status==true %>
<div class="grouppending">


<p>以下のグループからの招待が来ています</p>
<% @members.each do |member| %>
<% if member.status!=true %>
  <div class="invitation">
  <p>
<%= Group.find(member.group_id).group_name %><br>
  <p class="shonin"><%= link_to "承認する", "/groups/#{member.group_id}/users/#{current_user.id}", method: :get %></p>
  </p>
  </div>
<% end %>
<% end %>
<% end %>
<%end %>
  </div>


  </div>

<div class="mygoalslist">
  <p class="user_title">個人の目標</p>
<% @goals.each do |goal| %>
<p class="goaleach">
  <%= link_to goal.goal_name, "/users/#{@user.id}/goals/#{goal.id}", method: :get %>
  </p>
<% end %>
  </div>


<div class="mygoalslist">
 <p class="user_title">グループの目標</p>
<% @group_id.each do |g| %>
<% @goals=Goal.where(group_id: g) %>
<% @goals.each do |goal| %>
<p class="goaleach">
 <%= link_to goal.goal_name, "/groups/#{g}/goals/#{goal.id}", method: :get %></p>
<% end %>
<% end %>
  </div>
